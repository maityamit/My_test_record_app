<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mytestbyamitmaity.example.mytestapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/kogooo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/kogooo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTestApp">
        <activity android:name=".SpalshActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ForgotPassword" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MarksListActivity" />
        <activity android:name=".SubjectsListActivity" />
        <activity android:name=".MainActivity"/>
    </application>

</manifest>